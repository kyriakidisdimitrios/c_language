#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
 task: write a function that creates amd fills with values a dynamic array,
the size of the array is UNKNOWN in advance.
what's known is that once we read a value of -1, the function should stop reading inputs from the user, and it should retun the array
 */

int main() {
 char a[];
 char input=NULL;
 scanf("", &input);
 while(scanf!=-1) {

 }
 return 1;

}